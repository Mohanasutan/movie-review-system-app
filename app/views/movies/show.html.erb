<p style="color: green"><%= notice %></p>

<%= render @movie %>
<% if current_user.id==1 %>
    <%= link_to "Edit this movie", edit_movie_path(@movie), class: "btn btn-info  btn-sm"  %>
    <br>
    <br>
    <%= button_to "Destroy this movie", @movie, method: :delete, class: "btn btn-info  btn-sm"  %>
<% end %>
<br>
<%= link_to "Back to movies", movies_path, class: "btn btn-info  btn-sm"  %>
