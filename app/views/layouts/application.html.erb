<!DOCTYPE html>
<html>
  <head>
    <title>MovieReviewSystemApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
  </head>

  <body>
  <div class="navbar" style="height: 60px;">
    <%= link_to "Home", root_path,class: "nav-link", class: "btn btn-outline-light"%>
    <% if current_user %>
      <div class="greet">
      <span class="greeting">Hello, <%= current_user.email %></span>
      </div>
      <%= link_to "Edit Profile", edit_user_registration_path, class: "btn btn-outline-light" %>
      <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "btn btn-outline-light" %>
    <% else %>
      <%= link_to "Sign In", new_user_session_path, class: "nav-link", class: "btn btn-outline-light" %>
      <%= link_to "Sign Up", new_user_registration_path, class: "nav-link", class: "btn btn-outline-light" %>
    <% end %>
  </div>

  <%= yield %>
  </body>
</html>

<style>
    .greet {
        border: 1px solid white; /* Add a white border */
        padding: 8px; /* Add padding for spacing inside the box */
        border-radius: 5px; /* Add rounded corners if desired */
        display: inline-block; /* Ensure it doesn't take up the entire width */
        color: white; /* Set the text color to white */
    }
</style>
